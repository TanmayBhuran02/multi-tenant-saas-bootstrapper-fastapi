# Multi-Tenant Bootstrapper — Alembic Configuration
#
# Copy this file and the entire `migrations/` directory into your project.
# Then update the sqlalchemy.url below to match your database.

[alembic]
script_location = %(here)s
prepend_sys_path = .
sqlalchemy.url = postgresql+asyncpg://saas:saas@localhost:5432/saas_bootstrapper
